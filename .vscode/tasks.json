{
    "tasks": [
        {
            "type": "cppbuild",
            "label": "C/C++: g++.exe build active file",
            "command": "C:\\msys64\\ucrt64\\bin\\g++.exe",
            "args": [
                "-fdiagnostics-color=always",
                "-g",
                "-std=c++17",
                "-I${workspaceFolder}/include",
                "-L${env:GLFW_LIB}",
                "${file}",
                "-o",
                "${fileDirname}\\varyag.exe",
                "-lglfw3",
                "-lopengl32", // Often needed for OpenGL projects
                "-lgdi32", // Often needed on Windows
                "-luser32", // Often needed on Windows
                "-lkernel32" // Often needed on Windows
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Task generated by Debugger."
        }
    ],
    "version": "2.0.0"
}